{
    "builds": [{
        "src": "DEXTER/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "DEXTER/wsgi.py"
        }
    ]
}